#!/bin/bash -e
DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && cd .. && pwd )
LIBS=$(echo ${DIR}/usr/lib)
LIBS=$LIBS:$(echo ${DIR}/usr/local/lib)
exec ${DIR}/lib*/ld-*.so* --library-path $LIBS ${DIR}/usr/bin/file "$@"
